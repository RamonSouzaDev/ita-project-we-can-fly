services:
  aero-defense:
    build: .
    container_name: ita_project_we_can_fly
    ports:
      - "8501:8501" # Expondo a porta do Streamlit
    volumes:
      - .:/app # Mount current dir to save charts/logs back to host
    command: python standalone_sim.py
